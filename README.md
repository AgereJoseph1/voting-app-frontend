# Voting App Frontend

Frontend for the voting app, built with Next.js and TypeScript.

## Getting Started

```bash
npm install
npm run dev
```

Set the backend API base URL via environment variable:

- `NEXT_PUBLIC_API_BASE_URL` – URL of the FastAPI backend (e.g., `http://localhost:8000`).

This README will be expanded as the implementation progresses.
